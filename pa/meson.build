project('CP-Problems', 'c',
  default_options : [
    'c_std=c17',
    'warning_level=3',
    'c_args=-Wpedantic -pedantic',
    'buildtype=release'
  ]
)

src = files('src/main.c')
executable('main', src)
